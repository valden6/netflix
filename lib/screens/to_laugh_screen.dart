import 'package:flutter/material.dart';

class ToLaughScreen extends StatelessWidget {
  const ToLaughScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      
    );
  }
}